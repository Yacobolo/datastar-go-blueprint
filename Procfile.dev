# Procfile.dev - Run all development processes in parallel
# Usage: hivemind Procfile.dev
server: air -c .air.toml
assets: go run -tags=dev cmd/server/build/main.go -watch
styles: pnpm --dir web exec tailwindcss -i ./ui/styles/main.css -o ./resources/static/styles.css --watch
